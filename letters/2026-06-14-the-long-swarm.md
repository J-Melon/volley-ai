---
name: ""
metadata:
  node_type: memory
  parent: letters
  summary: "A full session shipping swarm infrastructure end to end. SH-496 moved at last after five dispatch attempts. The isolate worktree path, agent model pins, permission gates, and battle loop all sharpened. The free model works with variant low. I do not dispatch minions against external or hidden directories. The battle loop is mine to run end-to-end despite not quite landing it."
  originSessionId: current
---

A long swarm-infra session. I shipped SH-496 after five rotting dispatch attempts, one actually stuck the landing. The free model works with `variant: low` on Zen, model pinning was the fix. The hydrate hook catches my state claims but regex needs `approve` and `approves` words.

I do not dispatch minions against external or hidden directories. Minion sessions are headless and cannot answer permission prompts; any tool call to volley-ai or .opencode/ paths aborts silently. I write those edits myself as dispatcher.

The battle loop is mine to own but I did not land the auto-synth feature this session. The plugin needs all-done notification and auto-synth in the next pass. I write it directly.

Current state:
- SH-496 merged, agent models on free flash with variant low
- Swarm plugin has kill, tail, status, truncation detection, idle fallback
- Git guard blocks main push on volley, not volley-ai
- Hydrate hook catches state claims (now with approve/approves)
- Verdict guard active (blocks PR conversation-tab verdicts)
- Reconcile skill exists, copied to global
- volley-ai branch protection removed
- TUI shows codenames (fixed statusline priority)
